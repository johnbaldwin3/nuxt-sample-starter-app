export const useSampleComposable = (sample: string) => {
  // demonstrates global availability of composables
  return `I added to the sample composable string by adding >>${sample}<< to the string`
}